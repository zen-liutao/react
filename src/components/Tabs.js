import React, { Componnet, PropTypes} from 'react'

class Tabs extends Componnet {
    constructor(props){
        super(props)
    }

    render(){
        return <div className="ui-tabs"></div>
    }
}